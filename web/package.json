{
  "name": "wb-account-sync-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "跨 App 数据目录打通的界面（TypeScript）",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc && node build.mjs",
    "smoke": "node smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
