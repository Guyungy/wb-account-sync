# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store

# Local artifacts and private operation records
*.log
dist/
dist-*/
build/
build-*/
outputs/
.coverage
htmlcov/
plan*.json
journal.json
.wb-account-sync-state
.wb-account-sync-locks/

# 例外：tests/fixtures 下的黄金夹具是**源码**，不是运行时生成的计划文件。
# 上面那条 `plan*.json` 会把 plan_golden.json 一起吞掉 ——
# 一旦被吞，夹具不入库，CI 上 Rust 的 plan_id 测试直接找不到文件而失败。
!tests/fixtures/*.json

# Go 构建产物（legacy-go 是参照实现，2026-09-21 起非主路径）
# 必须盯着：输出路径写法一变就会漏。
# 注意：目录形态的忽略规则会连目录里的源码一起吞掉，
# 所以这里一律写成"具体到可执行文件名"的完整路径，不要用 **/wb-bridge。
legacy-go/wb-bridge
legacy-go/cmd/wb-bridge/wb-bridge
legacy-go/**/*.test

# Rust 构建产物（主底座；P0 之后生效）
target/
crates/*/target/
web/node_modules/
web/dist/

# Project working memory (daily logs / user-specific notes)
.workbuddy/
